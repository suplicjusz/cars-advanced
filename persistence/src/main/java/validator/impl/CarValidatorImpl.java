package validator.impl;

import model.car.Car;
import validator.CarValidator;

public class CarValidatorImpl implements CarValidator {
    @Override
    public Car validate(Car car) {
        return null;
    }
}
